// Committed, JSON-serializable dummy data for the reference fixture. No
// functions, no live services, no timestamps — every value survives a
// JSON round-trip, so the manifest can carry the same values as props.
export const LABELS = {
  save: 'Save',
  modalTitle: 'Delete account?',
  modalBody: 'This cannot be undone.',
  empty: 'No items yet',
  loading: 'Loading…',
  error: 'Something went wrong',
};
