# Confidence ledger proof (#399)

Generated by `generateStyleMapReport` from a synthetic, privacy-clean bundle
(two captured surfaces, one reasoned exclusion, one persisted auth wall). The
certification block leads the report; the visual verdict below it stays a
separate badge.

With a persisted crawl ledger carrying an unacknowledged auth wall:

```markdown
**Certification**

- **Coverage** — ✓ complete (2 of 3 registered surface(s) captured; 1 explicitly excluded)
- **Determinism** — ✓ proven (base self-checked, head self-checked)
- **Inventory** — ✓ navigable set unchanged
- **Confidence** — ⚠ limited (2 captured, 1 excluded-with-reason, 1 inaccessible); inaccessible: /login·password-input

✓ No reviewable computed-style changes among semantically matched elements. Content/structure was not evaluated.
```

Same bundle without the persisted wall — a reasoned exclusion alone still
reads limited, never a certified full green:

```markdown
- **Confidence** — ⚠ limited (2 captured, 1 excluded-with-reason)
```

And the machine-readable summary written to `report.json`:

```json
{
  "confidence": {
    "counts": {
      "inaccessible": 1,
      "unproven-determinism": 0,
      "excluded-with-reason": 1,
      "unknown": 0,
      "captured": 2
    },
    "completeness": "limited"
  }
}
```

A bundle from before the ledger existed degrades instead of blocking:

```markdown
- **Confidence** — ⚠ unknown (capture predates the confidence ledger; not blocking retroactively)
```
